#include <stdio.h>
/**
 * main - main Entry
 * Description: Print all possible combo of single num
 * Return: 0
 */
int main(void)
{
	int i = 0;

	while (i < 0)
	{
		putchar(i + '0');
		if (1 < 9)
		{
			putchar(44);
			putchar(32);
		}
		i++;
	}
	putchar('\n');

	return (0);
}
