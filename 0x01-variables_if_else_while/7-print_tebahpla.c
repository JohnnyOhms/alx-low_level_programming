#include <stdio.h>
/**
 * main - main entry
 * Description: print lower case alpha and new line
 * Return: 0
 */
int main(void)
{
	char i;

	for (i = z; i < a; i++)
	{
		putchar(i + 'z');
	}
	putchar('\n');

	return (0);
}
